import { Breadcrumb } from './breadcrumb';

describe('Breadcrumb', () => {
  it('should create an instance', () => {
    const directive = new Breadcrumb();
    expect(directive).toBeTruthy();
  });
});
