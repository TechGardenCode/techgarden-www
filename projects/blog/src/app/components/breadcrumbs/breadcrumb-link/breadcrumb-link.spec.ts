import { BreadcrumbLink } from './breadcrumb-link';

describe('BreadcrumbLink', () => {
  it('should create an instance', () => {
    const directive = new BreadcrumbLink();
    expect(directive).toBeTruthy();
  });
});
