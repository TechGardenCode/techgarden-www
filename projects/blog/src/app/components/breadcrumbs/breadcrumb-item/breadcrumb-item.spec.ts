import { BreadcrumbItem } from './breadcrumb-item';

describe('BreadcrumbItem', () => {
  it('should create an instance', () => {
    const directive = new BreadcrumbItem();
    expect(directive).toBeTruthy();
  });
});
